#include "api.hpp"

Result run_simulation(int cycles, unsigned l1CacheLines, unsigned l2CacheLines, unsigned cacheLineSize, unsigned l1CacheLatency, unsigned l2CacheLatency, unsigned memoryLatency, size_t numRequests, Request *requests, const char *tracefile)
{
    // TODO: implement function
}
